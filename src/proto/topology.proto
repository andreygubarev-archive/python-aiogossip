syntax = "proto3";

message Address {
    bytes ip = 1;
    int32 port = 2;
}

message Endpoint {
    Address addr = 1;

    bool reachable = 2;
    int32 latency = 3;
}

message Node {
    bytes id = 1;

    repeated Endpoint endpoints = 2;
}
